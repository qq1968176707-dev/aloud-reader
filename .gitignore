node_modules/
dist/
dist-electron/
dist-web/
release/
*.log
.DS_Store
samples/*.zip
.eb-cache/
# 图标：源是 build/icon.html，`npm run icon` 生成 png/icns/ico。
# 仓库根的 .gitignore 忽略了所有 build/，这里先放行目录再挑文件——icns/ico 必须进仓，
# 否则另一台机器打包时 electron-builder 找不到图标。
!build/
build/*
!build/icon.html
!build/icon.icns
!build/icon.ico

# 语音模型运行时：Kokoro 与 VoxCPM 的 venv + 权重（后者含 1.3GB 单文件，
# 远超 GitHub 100MB 上限），都能按 README 重装，绝不进仓。
tts-server/runtime/
tts-server/runtime-voxcpm/

# 自检产物与设计评审截图，每次 smoke 重新生成
smoke-result.json
smoke-stdout*.txt
shot-*.png

# 解析基线：记着本机书库的书名与绝对路径，属于个人数据。
# 每台机器自己 `node scripts/parity.mjs --record` 生成。
parity-baseline.json
